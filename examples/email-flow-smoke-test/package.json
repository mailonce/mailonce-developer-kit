{
  "name": "mailonce-email-flow-smoke-test",
  "version": "1.0.0",
  "private": true,
  "description": "Send OTP and verification-link test emails to a MailOnce inbox using your own SMTP provider.",
  "type": "module",
  "scripts": {
    "send": "node src/send-test-email.js",
    "check": "node --check src/send-test-email.js"
  },
  "dependencies": {
    "dotenv": "^16.6.1",
    "nodemailer": "^7.0.6"
  },
  "engines": {
    "node": ">=20"
  }
}
